import './globals.css'
export const metadata = { title: 'Calculette Carte Grise & Écotaxe' }
export default function RootLayout({ children }) {
  return <html lang="fr"><body>{children}</body></html>
}
